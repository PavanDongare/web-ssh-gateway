/**
 * Terminal component constants
 * Centralized definitions matching vibetunnel's approach.
 */

export const TERMINAL_IDS = {
  TERMINAL_CONTAINER: 'terminal-container',
} as const

/**
 * Standard terminal font family — comprehensive monospace fallback stack.
 */
export const TERMINAL_FONT_FAMILY =
  'ui-monospace, SFMono-Regular, "SF Mono", Consolas, "Liberation Mono", Menlo, monospace'
